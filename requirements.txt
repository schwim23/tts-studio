# --- Core TTS (keyless default) ---
edge-tts>=7.0.0

# --- Input extractors ---
pymupdf>=1.24.0            # PDF text extraction
python-docx>=1.1.0         # Word (.docx) extraction
trafilatura>=1.12.0        # URL -> clean article text
youtube-transcript-api>=0.6.2

# --- Audio merging ---
pydub>=0.25.1              # needs system ffmpeg (see packages.txt)

# --- Web UI ---
streamlit>=1.35.0

# --- Optional upgrade engine (enabled when a Google key is present) ---
google-cloud-texttospeech>=2.16.0
